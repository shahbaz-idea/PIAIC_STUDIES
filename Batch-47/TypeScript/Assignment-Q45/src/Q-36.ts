function make_shirt(size: string, message: string): void {
    console.log(`You have ordered a ${size} shirt with the message "${message}" printed on it.`);
}

make_shirt("medium", "I love my country!");

export {};