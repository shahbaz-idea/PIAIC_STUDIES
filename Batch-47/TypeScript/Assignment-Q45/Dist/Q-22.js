"use strict";
let lstArrsData = ["Ravi", "Satluj", "Sindh", "Jehlem", "Chinab"];
console.log("=================");
for (let index = 0; index < lstArrsData.length + 1; index++) {
    const element = lstArrsData[index];
    console.log(element);
}
console.log("=================");
for (let index = 0; index < lstArrsData.length; index++) {
    const element = lstArrsData[index];
    console.log(element);
}
